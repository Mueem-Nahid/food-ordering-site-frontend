import * as React from "react";
import Box from "./Box";

const TopBar: React.FC = () => {
  return (
    <div className="top-bar">
      <Box />
      <Box />
      <Box />
      <Box />
      <Box />
    </div>
  );
};

export default TopBar;
